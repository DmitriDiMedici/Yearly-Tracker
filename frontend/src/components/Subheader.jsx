import React from "react";

function Subheader({ currentYear }) {
  return (
    <div className="container content-title">
      <h2>"Placeholder" read in {currentYear}</h2>
    </div>
  );
}

export default Subheader;
